import { Button } from '@mantine/core';
import { IconEdit } from '@tabler/icons-react';

const UserEditButton = () => {
  return <Button rightSection={<IconEdit />}>Edit Info</Button>;
};

export default UserEditButton;
